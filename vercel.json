{
    "buildCommand": "npm install --legacy-peer-deps && bash ./create-symlinks.sh && npm run vercel-build",
    "framework": "nextjs",
    "functions": {
        "app/api/**/*.ts": {
            "maxDuration": 10
        }
    },
    "outputDirectory": ".next"
}